import * as React from 'react';
declare type Props = {
    label: React.ReactNode;
    meta?: React.ReactNode;
    size?: 'small' | 'medium';
    value: React.ReactNode;
};
export declare const Stat: ({ label, meta, size, value }: Props) => JSX.Element;
export {};
